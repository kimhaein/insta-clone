extends layouts/main
include mixins/contentBlock

block content
    .centerBox.loginBox
        h1 로그인
        .errorMsg #{loginErrMsg}
        form(action=routes.LOGIN, method='post')
            input(
                type='text'
                name='email'
                placeholder='이메일을 입력하세요'
            )
            input(
                type='password'
                name='password'
                placeholder='비밀번호를 입력하세요'
            )
            button(type='submit') 로그인
        a(href='/join') 아직 회원이 아니신가요?